import { memo } from 'react';
import clsx from 'clsx';
import style from './index.module.scss';

const ActionArea = memo(() => {
  return (
	<div className={clsx(style.container)}>
	  
	</div>
  );
});

export default ActionArea;